Put today's Pharma_pending_orders.xlsx file in THIS folder, named exactly:

    pending_orders.xlsx

(lowercase, underscore, no date/timestamp in the name — the dashboard always
looks for this exact filename). Every morning, just replace this file with
the latest export and re-upload/commit it to your hosting — anyone who opens
or refreshes the dashboard link will automatically see the new data within a
few seconds, no manual import needed.
